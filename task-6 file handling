1 struct employee
/*#include <stdio.h>
struct Employee
{
    int empid;
    char name[30];
    float monthlySalary;
};
   void calculateAnnualSalary(struct Employee emp)
{
    float annualSalary;
    annualSalary = emp.monthlySalary * 12;
 printf("\nEmployee Details\n");
    printf("Employee ID : %d\n", emp.empid);
    printf("Employee Name : %s\n", emp.name);
    printf("Monthly Salary : %.2f\n", emp.monthlySalary);
    printf("Annual Salary : %.2f\n", annualSalary);
}
int main()
{    struct Employee emp;
   printf("Enter Employee ID: ");
    scanf("%d", &emp.empid);
    printf("Enter Employee Name: ");
    scanf("%s", emp.name);
    printf("Enter Monthly Salary: ");
    scanf("%f", &emp.monthlySalary);
    calculateAnnualSalary(emp);
    return 0;
}*/
//2 creat an union 
/*#include<stdio.h>
union data {
    int i;
    char c;
    float f;
};
int main(){
    union data d;
    d.i=100;
    printf("integer :%d\n",d.i);
    d.c='K';
    printf("character :%ch\n",d.c);
    d.f=35.5;
    printf("float :%ch\n",d.f);
    printf("after storing character\n");
     printf("integer: %d\n",d.i);
      printf("character:%ch\n",d.c);
       printf("float:%.2f\n",d.f);
       return 0;
    
}*/
//3 enum representation menu
/*#include<stdio.h>
#include<string.h>
enum menu 
{
    add=1,
    subtract,
    multiply,
    divition,
};
int main(){
    int choice;
    float a,b;
    printf("1.addition\n");
    printf("1.subtract\n");
    printf("1.multiply\n");
    printf("1.divition\n");
    printf("Enter your choice: ");
    scanf("%d", &choice);
printf("Enter two numbers: ");
    scanf("%f %f", &a, &b);
    switch (choice)
    {
        case 1: add:
            printf("Result = %.2f\n", a + b);
            break;
        case 2: subtract:
        printf("Result = %.2f\n", a - b);
            break;
        case 3:Multiply:
            printf("Result = %.2f\n", a * b);
            break;
        case 4:divition:
            if (b != 0)
                printf("Result = %.2f\n", a / b);
            else
            printf("division by zero is not possible\n");
            break;
            default:
            printf("invalid choice\n");
            }
            return 0;
}*/
//4 #define calculate area of the circle
/*# include<stdio.h>
# define PI 3.141

int main(){
    float radius,area;
    printf("enter the radius:  ");
    scanf("%f",&radius);
    area= PI*radius*radius;
    printf("radius=%.2f\n",radius);
    printf("area of circle =%.2f\n", area);
    return 0;
}*/

//5# indef debug

/*#include <stdio.h>
#define DEBUG
int main()
{
    int a = 10, b = 20;
    int sum;
    sum = a + b;
#ifdef DEBUG
    printf("Debug Information\n");
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("sum = %d\n", sum);
#endif
    printf("result = %d\n",sum);
    return 0;
}*/
//7 errno,perror(),strerror()
/*#include <stdio.h>
#include <errno.h>
#include <string.h>
int main()
{
    FILE *fp;
    fp = fopen("sample.txt", "r");
    if (fp == NULL)
    {
        perror("Error");
        printf("Error Number: %d\n", errno);
        printf("Error Message: %s\n", strerror(errno));
    }
    return 0;
}*/

//8 set the nth bit of an integer
 /*#include <stdio.h>
int main()
{    int num, n;
   printf("Enter number: ");
    scanf("%d", &num);
    printf("Enter bit position: ");
    scanf("%d", &n);
    num = num | (1 << n);
    printf("Result = %d\n", num);
    return 0;
}*/
//9)clear the nth bit of an integer
/* #include <stdio.h>
int main()
{    int num, n;
   printf("Enter number: ");
    scanf("%d", &num);
    printf("Enter bit position: ");
    scanf("%d", &n);
    num = num & ~(1 << n);
    printf("Result = %d\n", num);
    return 0;
}*/

//10)toggle the nth bit of an integer
/* #include <stdio.h>
int main()
{
    int num, n;
    printf("Enter number: ");
    scanf("%d", &num);
printf("Enter bit position: ");
    scanf("%d", &n);
    num = num ^ (1 << n);
    printf("Result = %d\n", num);
 return 0;
}*/
//11)nth bit are set or not
/*#include <stdio.h>
int main()
{
    int num, n;
    printf("Enter number: ");
    scanf("%d", &num);
printf("Enter bit position: ");
    scanf("%d", &n);
    if (num & (1 << n))
        printf("Bit is SET\n");
    else
        printf("Bit is NOT SET\n");
    return 0;
}*/

//12)calloc()using malloc()
/* #include <stdio.h>
#include <stdlib.h>
int main()
{
    int *ptr, n, i;
    printf("Enter number of elements: ");
    scanf("%d", &n);
    ptr = (int *)malloc(n * sizeof(int));
    for(i = 0; i < n; i++)
        ptr[i] = 0;
    printf("Elements:\n");
    for(i = 0; i < n; i++)
        printf("%d ", ptr[i]);
    free(ptr);
    return 0;
}*/
//13)structure dynamically using malloc()
/* #include <stdio.h>
#include <stdlib.h>
struct Student
{
    int id;
    char name[30];
};

int main()
{
    struct Student *s;
    s = (struct Student *)malloc(sizeof(struct Student));
    printf("Enter ID: ");
    scanf("%d", &s->id);
    printf("Enter Name: ");
    scanf("%s", s->name);
    printf("\nID = %d\n", s->id);
    printf("Name = %s\n", s->name);
    free(s);
    return 0;
}*/
//14)linear search
 /*#include <stdio.h>
int main()
{    int a[100], n, key, i, found = 0;
   printf("Enter size: ");
    scanf("%d", &n);
    printf("Enter elements:\n");
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);
    printf("Enter element to search: ");
    scanf("%d", &key);
    for(i = 0; i < n; i++)
    {
        if(a[i] == key)
        {
            found = 1;
            break;
        }
    }
    if(found)
        printf("Element found at position %d", i + 1);
    else
        printf("Element not found");
    return 0;
}*/
//15bubble sort
/*#include <stdio.h>
int main()
{    int a[100], n, i, j, temp;
   printf("Enter size: ");
    scanf("%d", &n);
    printf("Enter elements:\n");
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);
    for(i = 0; i < n - 1; i++)
    {
        for(j = 0; j < n - i - 1; j++)
        {
            if(a[j] > a[j + 1])
            {
                temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }
    printf("Sorted Array:\n");
    for(i = 0; i < n; i++)
        printf("%d ", a[i]);
    return 0;
}*/
//6 text file and student file
/*#include <stdio.h>
int main()
{
    FILE *fp;
    int rollNo;
    char name[30];
    float marks;
    fp = fopen("student.txt", "w");
    if (fp == NULL)
    {
        printf("File cannot be created.\n");
        return 1;
    }
    printf("Enter Roll Number: ");
    scanf("%d", &rollNo);
    printf("Enter Name: ");
    scanf("%s", name);
    printf("Enter Marks: ");
    scanf("%f", &marks);
    fprintf(fp, "Roll Number : %d\n", rollNo);
    fprintf(fp, "Name : %s\n", name);
    fprintf(fp, "Marks : %.2f\n", marks);
    fclose(fp);
    printf("Student details written to student.txt successfully.\n");
    return 0;
}*/
