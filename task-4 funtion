// 1) strlen()
/*#include <stdio.h>
#include <string.h>
int main(){
    char ch [30];
    printf("enter the value ");
    fgets(ch,30,stdin);
    printf("%d",strlen(ch));
    return 0;
    }*/
     // 2) strcpy()
/*#include <stdio.h>
#include <string.h>
int main(){
    char str1[30];
    char str2[50];
    printf("enter your data ");
    scanf("%s %s", str1 ,str2);
    printf("before copy %s\n",str1);
    strcpy(str1,str2);
    printf("after copy %s\n",str1);
    return 0;
}*/

// 3)strcmp()
/*#include <stdio.h>
#include <string.h>
int main(){
    char str1[10];
    char str2[30];
    printf("enter the number  ");
    scanf("%s %s",str1,str2);
    if (strcmp(str1,str2)<0){
        printf("%s  is less than \n ",str1);
    } else if
     (strcmp(str1,str2)>0){
        printf("%s is greater than str1\n", str2);
     }else
     {
        printf("is  both equal  \n");
     }
        return 0;
    }*/
    //4) square root funtion
    
  /*  #include <stdio.h>
    #include<string.h>
    #include<math.h>
    int main(){
        float n;
    printf("enter the number  ");
    scanf("%f",&n);
    printf("square root=%2f",sqrt(n) );
    return 0;
    }*/
5.uppercase lowercase
/*#include <stdio.h>
#include<string.h>
#include<ctype.h>
int main() {
     char str[25];
     char str1[30];
    printf("enter your names: ");
    scanf(" %s %s",str,str1);
    for(int i=0; str[i]!='\0'; i++){
        str[i]=tolower(str[i]);
        }
    printf("%s\n",str);
    for(int i=0; str1[i]!='\0'; i++){
        str1[i]=tolower(str1[i]);
       }
     printf("%s\n",str1);
    if(strcmp(str,str1)==0){
        printf("both are same employee\n");
    }else
        printf("both are different employee");
    return 0;
}*/
//6)
/*#include <stdio.h>
int largest(int a, int b);
int main(){
    int x,y,result;
    printf("enter two numbers ");
    scanf("%d %d",&x,&y);
    result=largest(x,y);
    printf("the largest number is %d\n",result);
    return 0;
    }
    int largest(int a,int b){
        if(a>b){
            return a;
        }
            else{
                return b;
            }
    }*/
    //7)  even or add
   /* #include <stdio.h>
int checkevenodd(int n);
int main(){
    int n,result;
    printf("enter a numbers ");
    scanf("%d",&n);
    result = checkevenodd(n);
    if (result==1){
    printf("the even num %d\n",n);
    }else{
        printf("the odd num %d\n",n);
    }
    return 0;
    }
    int checkevenodd(int n){
        if(n%2==0){
            return 1;
        }
            else{
                return 0;
            }
    }*/
    //8)sawp()
   /* #include <stdio.h>
void swap(int*n,int*m);
int main(){
    int x,y;
    printf("enter two numbers ");
    scanf("%d %d",&x,&y);
    printf("before swap the number %d,%d\n",x,y);
    swap(&x,&y);
    printf("after swap the number %d,%d\n",x,y);
    return 0;
    }
    void swap(int*n,int*m){
      int temp;
      temp=*n;
      *n=*m;
      *m=temp;
    }*/
    //9)array and return the maximum element
    /*#include<stdio.h>
    int maximum (int arr[],int n)
    {
     int i,max=arr[0];
        for(i=1;i<n;i++){
            if(arr[i]>max)
            max=arr[i];
        }
        return max;
    }
    int main(){
        int arr[50],n,i;
        printf("enter size: ");
        scanf("%d",&n);
        printf("enter elements:\n");
        for(i=0;i<n;i++)
        scanf("%d",&arr[i]);
        printf("maximum=%d",maximum(arr,n));
        return 0;
    }*/
    //10)shopping bill
   /* #include <stdio.h>
float total(float a){
    return a;
}
float gst(float a){
    return a*0.18;
}
float finalBill(float a){
    return a+gst(a);
}
int main(){
float amount;
printf("Enter total amount: ");
    scanf("%f",&amount);
printf("\nTotal Amount = %.2f", total(amount));
    printf("\nGST = %.2f", gst(amount));
    printf("\nFinal Bill = %.2f", finalBill(amount));
return 0;
}*/
//11)array and its position
/*#include <stdio.h>
int search(int arr[],int n,int key){
 for(int i=0;i<n;i++){
        if(arr[i]==key)
            return i;
    }
 return -1;
}int main(){
    int arr[100],n,key,i,pos;
  printf("Enter size: ");
    scanf("%d",&n);
    printf("Enter elements:\n");
for(i=0;i<n;i++)
        scanf("%d",&arr[i]);
        printf("Enter element to search: ");
    scanf("%d",&key);
pos=search(arr,n,key);
    if(pos==-1)
        printf("Element not found");
    else
        printf("Element found at position %d",pos+1);
    return 0;
}*/
//12)N natural numbers using recursive ()
/*#include <stdio.h>
int sum(int a){
    if(a==1)
        return 1;
return a+sum(a-1);
}
int main(){
    int a;
    printf("Enter the value ");
    scanf("%d",&a);
printf("Sum = %d",sum(a));
    return 0;
}*/
//13)to print 1-N
 /*#include <stdio.h>
void print(int n){
    if(n==0)
        return;
print(n-1);
    printf("%d ",n);
}
int main(){
int n;
 printf("Enter the num: ");
    scanf("%d",&n);
    print(n);
    return 0;
}*/
//14) Nth fibonacci number
/* #include <stdio.h>
int fib(int n){
    if(n==0)
        return 0;
if(n==1)
        return 1;
    return fib(n-1)+fib(n-2);
}
int main(){
    int n;
printf("Enter the number: ");
    scanf("%d",&n);
    printf("%d",fib(n));
    return 0;
}*/
//15) staircase recursive funtion of nth step
/* #include <stdio.h>
int base(int n){
    if(n==0 || n==1)
        return 1;
 return base(n-1)+base(n-2);
}
int main()
{    int n;
   printf("Enter the num : ");
    scanf("%d",&n);
    printf("Total base = %d",base(n));
return 0;
}*/

